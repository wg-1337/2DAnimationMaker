{
  "name": "@animation-maker/plugin-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "插件开发 SDK — 类型定义 + CLI脚手架",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "create-am-plugin": "./src/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/ --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@animation-maker/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
